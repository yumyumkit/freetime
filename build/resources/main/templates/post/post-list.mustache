{{>layout/headernon}}

<h1 style="font-weight:bold; text-align:center">미美취러들이 정보를 공유하는 방법</h1><br/><br/>
<div>
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>제목</th>
            <th>작성자</th>
            <th>등록일</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}
            <tr>
                <td><a href="/posts/{{id}}" style="color: #000000">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>

    <ul class="pagination justify-content-center">
        <li class="page-item"><a class="page-link" href="?page={{previous}}">이전</a></li>
        {{#check}}
            <li class="page-item"><a class="page-link" href="?page={{next}}">다음</a></li>
        {{/check}}
        {{^check}}
            <li class="page-item disabled"><a class="page-link" href="?page={{next}}">다음</a></li>
        {{/check}}
    </ul>
    <a href="/posts/save" role="button" class="btn btn-light">정보 공유하기</a>
</div>
{{>search/partySearchForm}}
{{>layout/footer}}